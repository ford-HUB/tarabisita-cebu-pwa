/** Guest cart snapshot (items + selection) while browsing public pages. */
export const GUEST_CART_STORAGE_KEY = 'tb_public_guest_cart'

/** Set when guest taps checkout; cleared after tourist session merges cart from storage. */
export const GUEST_CART_CHECKOUT_PENDING_KEY = 'tb_public_guest_cart_checkout_pending'

export const publicCartHref = '/cart'
